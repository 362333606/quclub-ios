name: QiuYuAI
options:
  bundleIdPrefix: cn.qyai001
  deploymentTarget:
    iOS: "15.0"
  createIntermediateGroups: true
settings:
  base:
    DEVELOPMENT_TEAM: KD737T5M5W
    CODE_SIGN_STYLE: Manual
    SWIFT_VERSION: "5.9"
targets:
  QiuYuAI:
    type: application
    platform: iOS
    sources:
      - path: QiuYuAI
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: cn.qyai001.quclub
        PRODUCT_NAME: 趣club
        INFOPLIST_FILE: QiuYuAI/Info.plist
        MARKETING_VERSION: "1.0.0"
        CURRENT_PROJECT_VERSION: "1"
        TARGETED_DEVICE_FAMILY: "1,2"
        IPHONEOS_DEPLOYMENT_TARGET: "15.0"
        CODE_SIGN_ENTITLEMENTS: QiuYuAI/QiuYuAI.entitlements
        CODE_SIGN_IDENTITY: "Apple Distribution"
        PROVISIONING_PROFILE_SPECIFIER: "QuClub AdHoc 20260904"
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor
        ENABLE_PREVIEWS: "NO"
        GENERATE_INFOPLIST_FILE: "NO"
        DEVELOPMENT_LANGUAGE: zh-Hans
schemes:
  QiuYuAI:
    build:
      targets:
        QiuYuAI: all
    run:
      config: Debug
    archive:
      config: Release
